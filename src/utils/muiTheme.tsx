import { createTheme } from "@mui/material/styles";
  
export const theme = createTheme({
    palette: {
      primary: {
        main: '#01565B'
      },
      secondary: {
        main: '#FFFFFF',
      },
    },
    typography: {
      fontSize: 16,
      fontFamily: 'Poppins'
    },
});